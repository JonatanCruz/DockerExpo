{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "bun",
            "name": "Launch via BUN",
            "request": "launch",
            "program": "${file}",
            "cwd": "${workspaceFolder}",
            "runtime": "bun"
        }
    ]
}